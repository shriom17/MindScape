flask==3.0.3
flask-cors==4.0.1
python-dotenv==1.0.1
sqlalchemy==2.0.31

# Optional ML / LLM stack is kept separate so the backend can start on a
# lightweight Python environment without building numpy/faiss/tensorflow.
